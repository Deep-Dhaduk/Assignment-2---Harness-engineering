# Project Memory

An original DeepSeek Harness Host plugin that registers `project_memory`. Notes persist to `assignment-project-memory.json` under `DSH_PROFILE_DIR` and support `add`, `list`, `search`, and `remove` actions.

The plugin has no install scripts, runtime dependencies, network calls, or command execution. Install this directory as a bundle from Creator Mode with `plugin_manager`.
